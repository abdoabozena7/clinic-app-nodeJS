# Copy this file to `.env` and adjust the values as needed

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=clinic_db
DB_PORT=3306

# JWT secret used for signing tokens (choose a strong, unpredictable value)
JWT_SECRET=your_jwt_secret

# Server listening port
PORT=5000